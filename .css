body {
    margin: 0;
    padding: 0;
    background: #0a0a0a;
    overflow: hidden;
    font-family: "Poppins", sans-serif;
    text-align: center;
}

/* Countdown */
.countdown {
    font-size: 120px;
    color: #ff4d6d;
    position: absolute;
    top: 40%;
    left: 50%;
    transform: translate(-50%, -50%);
    animation: pop 1s infinite;
}
@keyframes pop {
    0% { transform: translate(-50%, -50%) scale(1); }
    100% { transform: translate(-50%, -50%) scale(1.3); }
}

/* hide */
.hidden {
    display: none;
}

/* NISA Glow */
.nisa-glow {
    font-size: 60px;
    color: #ff99cc;
    margin-top: 40px;
    text-shadow: 0 0 15px #ff1e8a, 0 0 30px #ff1e8a;
    animation: glow 1.5s infinite alternate;
}
@keyframes glow {
    0% { text-shadow: 0 0 10px #ff1e8a; }
    100% { text-shadow: 0 0 30px #ff1e8a; }
}

/* Text animation */
.text-seq {
    margin-top: 30px;
    font-size: 32px;
    color: #fff;
    min-height: 60px;
}

/* Heart shape */
.heart {
    width: 220px;
    height: 200px;
    background-color: #ff4d6d;
    position: absolute;
    left: 50%;
    top: 58%;
    transform: translate(-50%, -50%) rotate(-45deg);
    border-radius: 60px 0 0 60px;
    animation: heartbeat 1s infinite;
}
.heart::before,
.heart::after {
    content: "";
    width: 220px;
    height: 200px;
    background-color: #ff4d6d;
    border-radius: 50%;
    position: absolute;
}
.heart::before { top: -110px; left: 0; }
.heart::after { left: 110px; top: 0; }

.heart p {
    transform: rotate(45deg);
    font-size: 22px;
    color: white;
    font-weight: bold;
    position: absolute;
    top: 80px;
    left: 20px;
    width: 180px;
    text-align: center;
}

/* Heartbeat */
@keyframes heartbeat {
    0% { transform: translate(-50%, -50%) rotate(-45deg) scale(1); }
    100% { transform: translate(-50%, -50%) rotate(-45deg) scale(1.1); }
}

/* Falling hearts */
#falling-hearts {
    position: fixed;
    width: 100%;
    height: 100%;
    pointer-events: none;
}
.falling-heart {
    position: absolute;
    color: pink;
    font-size: 20px;
    animation: fall 3s linear infinite;
}
@keyframes fall {
    0% { transform: translateY(-50px); }
    100% { transform: translateY(110vh); }
}